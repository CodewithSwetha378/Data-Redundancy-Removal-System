# Data Redundancy Removal System

This is a python application that manages a user database using SQLite and prevents duplicate from being added.

## Features
* Add new users(Name and Email)
* Automatically checks for duplicate emails
* View all registered users
